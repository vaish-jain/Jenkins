pipeline {
    agent any
    stages{
        stage('Git Checkout'){
          steps{
           
             git credentialsId: 'gitcred', url: 'https://github.com/vaish-jain/Jenkins.git'
          }
        }
        
    }
}
